# 아파트 통근 스크리너

상세 문서는 [`apt_screener/README.md`](apt_screener/README.md) 를 보세요.

```bash
cd apt_screener
pip install -r requirements.txt
export PYTHONPATH=src
python3 -m apt_screener screen --demo --offline
```
